html
    head
        title User Details
    body
        div
            div user keys: #{Object.keys(user)}
            div user values: #{Object.values(user)}
            //- div displayName: #{user.displayName}
            //- div email: #{user.emails[0].value}
            //- div givenName: #{user.name.givenName}
            //- img(src=user.photos[0].value, referrerpolicy='no-referrer')
        
